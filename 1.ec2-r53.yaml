- name: create ec2 and route53
  hosts: local
  connection: local
  vars:
    ami_id: "ami-0220d79f3f480ecf5"
    sg_id: "sg-0961a404cd03a451a"
    zone_id: "Z09150472BEG29NERJFM7"
    domain_name: "dso86s.xyz"
    instances:
    - mongodb 
  tasks:
  - name: create ec2 instance 
    amazon.aws.ec2_instance:
      name: "{{ item }}"
      instance_type: t3.micro
      security_group: "{{ sg_id }}"
      image_id: "{{ ami_id }}"
      tags:
        Name: "{{ item }}"
    loop: "{{ instances }}"
    register: ec2_output

  - name: print the ec2_output
    ansible.builtin.debug:
      msg: "{{ ec2_output }}"

  # - name: print public IP
  #   ansible.builtin.debug:
  #     msg: "{{ ec2_output.results[0].instances[0].public_ip_address }}"
  #   # loop: "{{ ec2_output.results }}"

  # - name: print private IP
  #   ansible.builtin.debug:
  #     msg: "{{ ec2_output.results[0].instances[0].private_ip_address }}"
    

  # - name: delete ec2 instance
  #   amazon.aws.ec2_instance:
  #     name: "{{ item }}"
  #     state: absent
  #   loop: "{{ instances }}"

  # - name: create route53 record 
  #   amazon.aws.route53:
  #     state: present
  #     zone: "{{ domain_name }}"
  #     record: "{{ item.item }}.{{ domain_name }}" # mongodb.dso86s.xyz
  #     type: A
  #     ttl: 1
  #     value: "{{ item.instances[0].private_ip_address }}" # private ip adding 
  #     overwrite: true
  #   loop: "{{ ec2_output.results }}"

  # - name: Delete a Route 53 DNS record
  #   amazon.aws.route53:
  #     state: absent
  #     zone: "{{ domain_name }}"
  #     record: "{{ item.item }}.{{ domain_name }}"
  #     type: A
  #     value: "{{ item.instances[0].private_ip_address }}"
  #   loop: "{{ ec2_output.results }}"